var y: Foo = 10; // Unresolved type;  walking the tree to check if Foo found to be defined
// otherwise we should throw unresolvedError; For now i think of Namebidning rather than typechecking;
var x: int = 10;
typealias Foo = int;  // Declaring type of Foo
var ff: int = 10;
var g: Foo  = 1;  // Correct type to Foo->int see line 3
// typealias Foo = int;  Error Redefinition see line 3 !