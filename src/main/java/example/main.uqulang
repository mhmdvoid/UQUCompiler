var globalVar: bool = false;
typealias Myint = int;
func int foo(bar: int, foobar: bool) {
    var bar: bool = false;
    // var globalVar: int = 10; // globalVar gets bound to localScope;
    var bar: int = 2;   // Error bar is already defined same scope;
}