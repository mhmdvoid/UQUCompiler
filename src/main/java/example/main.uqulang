typealias Foo = int;
typealias Bar = bool;

var x: Bar = 10;   // will give error `x` type not match with expression Int type
var y: FooBar = false;   // will give error `FooBar` doesn't exist and default initialExpression to int type; Undeclared Error
var u: Foo = 10;  // Works fine;
func int funcDecl(param: Foo)   // param's type now is a typealias with sema analysis applied 'int'
{}
