package semantic.type_checking;

public class TypeCheckExpr {
    // Todo
    // ExprVisitor/Walker
    // Walking Exprs to resolve their type, Luckily we don't support complex type-checking algorithms. All decls && operators are clear no operator overloading no type inference.
    // = funcCall();  <- message call with already-given type
    // = 10 + 10 * 10 / 2; also all operators here know what operands' types should be.
}
